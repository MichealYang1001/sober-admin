# Sober Admin

Internal Sober permission approval and student entitlement console.

## Local Development

```bash
npm install
npm run dev
```

The app runs on `http://localhost:3003` and calls `NEXT_PUBLIC_SOBER_API_BASE_URL`, defaulting to `http://localhost:5002`.
